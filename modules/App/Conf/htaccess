<IfModule mod_rewrite.c>

    // Tell PHP that the mod_rewrite module is ENABLED.
    SetEnv HTTP_MOD_REWRITE On

    RewriteEngine On
    RewriteBase @rewriteBase@
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule . @baseDir@/index.php [L]

</IfModule>