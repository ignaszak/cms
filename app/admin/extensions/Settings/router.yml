routes:

    admin-settings-list:
        path: '/admin/settings/{setting}'
        method: GET
        controller: AdminController\Settings\ViewSettingsController
        group: Settings
        tokens:
            setting: general

    admin-settings-save:
        path: '/admin/settings/save'
        method: POST
        controller: AdminController\Settings\SaveSettingsController
        group: Settings

